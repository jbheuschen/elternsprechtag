{{ form_start(form) }}
    <div class="row">
        <div class="col-sm-7">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-subtitle mb-2 text-muted">Persönliche Angaben</h6>
                    <div class="form-group">
                        {{ form_widget(form.name) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form.surname) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form.email) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form.message) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="col-sm-5">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-subtitle mb-2 text-muted">Lehrerwahl</h6>
                    <div class="form-group">
                        {{ form_widget(form.teacher) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form.slot) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="ptop">
        <button class="btn btn-primary">{{ button_label|default('Termin vereinbaren') }}</button>
    </div>
{{ form_end(form) }}
