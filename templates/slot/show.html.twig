{% extends 'base.html.twig' %}

{% block title %}Slot{% endblock %}

{% block body %}
    <h1>Slot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slot.id }}</td>
            </tr>
            <tr>
                <th>SlotBegin</th>
                <td>{{ slot.slotBegin ? slot.slotBegin|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>SlotEnd</th>
                <td>{{ slot.slotEnd ? slot.slotEnd|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slot_index') }}">back to list</a>

    <a href="{{ path('slot_edit', {'id': slot.id}) }}">edit</a>

    {{ include('slot/_delete_form.html.twig') }}
{% endblock %}
